services:
  ros2_jazzy:
    container_name: ros2_jazzy_compose
    stdin_open: true
    tty: true
    network_mode: "host"
    environment:
      - TERM=xterm-256color
    volumes:
      - ./workspace:/workspace
    build:
      context: .
      network: default
